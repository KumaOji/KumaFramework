<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kuma.cloud.blog.mapper.ReadyMapper">

    <update id="deleteByIdLogic">
        UPDATE ready SET status = 3 WHERE id = #{id}
    </update>

    <delete id="deleteByIdPhysical">
        DELETE FROM ready WHERE id = #{id}
    </delete>

</mapper>
