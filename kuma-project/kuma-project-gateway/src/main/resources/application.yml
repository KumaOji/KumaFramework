spring:
  profiles:
    active: ${active}
  application:
    name: gateway
  data:
    redis:
      host: ${redis_host}
      port: ${redis_port}
      database: ${redis_database}
      password: ${redis_password}
