plugins {
    id 'java-library'
    alias(projectLibs.plugins.springboot)
    alias(projectLibs.plugins.spring.dependency.management)
}

description = 'Myframework 启动类完整模块'

def springBootVersion = projectLibs.versions.springboot.get()
dependencyManagement {
    imports {
        mavenBom "org.springframework.boot:spring-boot-dependencies:${springBootVersion}"
    }
}

dependencies {
    api project(':myframework-cloud-framework:myframework-module-infrastruct')
}

bootJar {
    enabled = false
}

jar {
    enabled = false
}
