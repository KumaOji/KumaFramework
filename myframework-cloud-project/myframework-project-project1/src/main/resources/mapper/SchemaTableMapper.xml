<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kuma.cloud.project1.mapper.SchemaTableMapper">

    <!-- 表名、排序字段需在 Service 层校验，仅允许 [a-zA-Z0-9_] -->
    <select id="listTables" resultType="java.lang.String">
        SELECT TABLE_NAME
        FROM information_schema.TABLES
        WHERE TABLE_SCHEMA = #{schemaName}
        ORDER BY TABLE_NAME
    </select>

    <select id="selectTablePage" resultType="com.kuma.cloud.project1.model.TableRow">
        SELECT * FROM `${tableName}`
        <if test="sort != null and sort != ''">
            ORDER BY `${sort}` ${order}
        </if>
    </select>

</mapper>
