plugins {
    id 'java-library'
}

description = 'KumaFramework 多租户 SaaS 模块（COLUMN / Schema / 数据源 三模式隔离，MybatisPlus 原生支持）'

dependencies {
    api project(':kuma-boot-framework:kuma-boot-starter-data-mybatis')

    api projectLibs.spring.boot.autoconfigure
    // spring-web 提供 OncePerRequestFilter / FilterRegistrationBean，不引入嵌入式 Tomcat
    api projectLibs.spring.web

    // 运行时由容器提供，仅编译期需要
    compileOnly projectLibs.jakarta.servlet.api
    compileOnly projectLibs.spring.cloud.context
    compileOnly projectLibs.jspecify
}
