plugins {
    id 'java-library'
}

description = 'Kumaframework 动态线程池模块（基于 DynamicTP）'

dependencies {
    api project(':kuma-boot-framework:kuma-boot-starter-core')

    // Spring Boot AutoConfiguration
    api projectLibs.spring.boot.autoconfigure

    // DynamicTP (DtpExecutor / ThreadPoolBuilder / ThreadPoolCreator / @EnableDynamicTp)
    api projectLibs.dynamic.tp.starter

    // Spring Cloud Context (@RefreshScope)
    compileOnly projectLibs.spring.cloud.context
}
