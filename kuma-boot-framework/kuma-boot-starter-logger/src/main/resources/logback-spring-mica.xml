<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="false" debug="false">
	<include resource="org/springframework/boot/logging/logback/defaults.xml"/>

	<contextListener class="com.kuma.boot.logger.logback.listener.TtlMDCAdapterListener"/>

	<property name="LOG_PATH" value="${user.home}"/>
	<property name="OS_NAME" value="${os.name}"/>
	<property name="OS_VERSION" value="${os.version}"/>
	<property name="USER_TIMEZONE" value="${user.timezone}"/>
	<property name="JAVA_VERSION" value="${java.version}"/>
	<property name="CURRENT_DATE" value="%d{yyyy-MM-dd}"/>

	<!-- 日志级别 -->
	<springProperty scope="context" name="loggingLevelRoot" source="logging.level.root"
					defaultValue="INFO"/>

	<springProperty name="KMC_VERSION"
					scope="context"
					source="kmcVersion"
					defaultValue="--"/>
	<springProperty name="APP_NAME"
					scope="context"
					source="spring.application.name"
					defaultValue="--"/>
	<springProperty name="SPRING_PROFILES_ACTIVE"
					scope="context"
					source="spring.profiles.active"/>
	<springProperty name="LOG_FILE"
					scope="context"
					source="logging.file.path"
					defaultValue="${LOG_PATH}/logs/${APP_NAME}"/>
	<springProperty name="LOG_MAX_FILE_SIZE"
					scope="context"
					source="logging.logback.rollingpolicy.max-file-size"
					defaultValue="2GB"/>
	<springProperty name="CLEAN_HISTORY_ON_START"
					scope="context"
					source="logging.logback.rollingpolicy.clean-history-on-start"
					defaultValue="true"/>
	<springProperty name="TOTAL_SIZE_CAP"
					scope="context"
					source="logging.logback.rollingpolicy.total-size-cap"
					defaultValue="7GB"/>
	<springProperty name="LOG_FILE_MAX_DAY"
					scope="context"
					source="logging.logback.rollingpolicy.max-history"
					defaultValue="30"/>
	<springProperty name="SERVER_IP"
					scope="context"
					source="spring.cloud.client.ip-address"
					defaultValue="0.0.0.0"/>
	<springProperty name="SERVER_PORT"
					scope="context"
					source="server.port"
					defaultValue="--"/>
	<springProperty name="BOOTSTRAP_SERVERS"
					scope="context"
					source="spring.kafka.bootstrap-servers"
					defaultValue="127.0.0.1:9092"/>
	<springProperty name="LOGSTASH_DESTINATION"
					scope="context"
					source="logging.extend.logstash-appender.destination"
					defaultValue="127.0.0.1:4560"/>

	<conversionRule conversionWord="clr"
					class="org.springframework.boot.logging.logback.ColorConverter"/>
	<conversionRule conversionWord="wex"
					class="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>
	<conversionRule conversionWord="wEx"
					class="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"/>
	<conversionRule conversionWord="wEx2"
					class="com.kuma.boot.logger.logback.converter.ExtendedWhitespaceThrowableProxyConverter"/>
	<conversionRule conversionWord="localIp"
					class="com.kuma.boot.logger.logback.converter.LocalIpConverter"/>

	<!-- 控制台日志 -->
	<include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

	<root level="${loggingLevelRoot}">
		<appender-ref ref="CONSOLE"/>
	</root>

	<!-- 减少部分debug日志 -->
	<logger name="org.springframework.context" level="WARN"/>
	<logger name="org.springframework.beans" level="WARN"/>
	<logger name="springfox.bean.validators" level="ERROR"/>
	<logger name="springfox.documentation" level="ERROR"/>
	<!-- 关闭 mybatis 默认的 sql 日志 -->
	<logger name="log.mybatis" level="INFO"/>
	<!-- 基础组件 -->
	<logger name="RocketmqClient" level="WARN"/>
	<logger name="com.alibaba.nacos" level="ERROR"/>
	<Logger name="org.reflections" level="ERROR"/>
	<!-- 请求日志打印，全部使用info，方便动态调整 -->
	<logger name="net.dreamlu.mica.servlet.logger" level="INFO"/>
	<logger name="net.dreamlu.mica.reactive.logger" level="INFO"/>
	<logger name="net.dreamlu.mica.http.logger" level="INFO"/>

	<springProfile name="dev | test">
		<!-- mongo no sql -->
		<logger name="org.springframework.data.mongodb.core" level="DEBUG"/>
		<!-- mica日志 -->
		<logger name="net.dreamlu.mica" level="INFO"/>
	</springProfile>

	<springProfile name="ontest">
		<!-- mica日志 -->
		<Logger name="net.dreamlu.mica" level="INFO"/>
	</springProfile>

	<springProfile name="prod">
		<!-- mica日志 -->
		<Logger name="net.dreamlu.mica" level="ERROR"/>
	</springProfile>

	<!-- https://logback.qos.ch/manual/configuration.html#shutdownHook and https://jira.qos.ch/browse/LOGBACK-1090 -->
	<shutdownHook class="ch.qos.logback.core.hook.DelayingShutdownHook"/>
	<contextListener class="ch.qos.logback.classic.jul.LevelChangePropagator">
		<resetJUL>true</resetJUL>
	</contextListener>

</configuration>
